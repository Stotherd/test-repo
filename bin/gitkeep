#!/usr/bin/env ruby

require 'gli'

module Gitkeep
  module CLI
    include GLI::App
    extend self

    program_desc 'GLI gitkeep'
    subcommand_option_handling :normal
    arguments :strict

    require_relative '../lib/gitkeep'

    exit run(ARGV)
  end
end
